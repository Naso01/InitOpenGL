#version 330

in vec4 retColor; // MUST use same name as out variable used by vertex shader
out vec4 FragColor;

uniform int RenderRedChannel;
uniform int RenderGreenChannel;
uniform int RenderBlueChannel;

void main()
{
	FragColor = retColor;
}